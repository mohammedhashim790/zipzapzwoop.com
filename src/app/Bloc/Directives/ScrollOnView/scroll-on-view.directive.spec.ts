import { ScrollOnViewDirective } from './scroll-on-view.directive';

describe('ScrollOnViewDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollOnViewDirective();
    expect(directive).toBeTruthy();
  });
});
